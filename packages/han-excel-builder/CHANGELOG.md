# Changelog

## [2.0.10] - 2026-02-02
### Fixed
- Ensure TypeScript declaration overloads for `ExcelReader.fromBuffer`/`fromFile` are preserved and usable by consumers.
- Use namespace imports for `exceljs` to avoid requiring `esModuleInterop` in consumers.
- Minor build fixes for declaration emission.

## [2.0.9] - previous
- Previous changes...